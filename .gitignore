__pycache__/
*.pyc
.DS_Store
.venv/
venv/
.pytest_cache/
.claude/

# Local-only context/handoff doc — must NOT be published to the public repo
HANDOFF.md

# Local-only design references (Claude design handoff bundle) — keep off the public repo
design/

# one-time trend backfill cache (regenerable; assembled into heat/data/trends.json)
data/trends_cache/
